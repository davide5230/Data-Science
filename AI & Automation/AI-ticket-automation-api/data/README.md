# Data

Runtime ticket data is stored locally in SQLite as:

```text
tickets.db
```

The database file is intentionally excluded from Git through `.gitignore`.

This directory is kept in the repository only to document where persistent local application data is created at runtime.
