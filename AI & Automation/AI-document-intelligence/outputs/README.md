# Outputs

Store generated structured results here.

After running the notebook, save a real example output as:

```text
sample_answer.json
```

The example should come from an actual notebook run rather than a manually invented result.
