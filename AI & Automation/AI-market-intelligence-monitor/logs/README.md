# Logs

Runtime application logs are written to this directory.

The monitor writes pipeline, scheduling, delivery and error events to:

`market_monitor.log`

Runtime log files are excluded from version control.
